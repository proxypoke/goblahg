Goblahg
-------

A simple blog written in Go, which uses flat files to store posts, which are
written with Markdown style syntax and stored in a single directory - which can
be a git repository, so you can add posts by simply pushing them from your
remote!
